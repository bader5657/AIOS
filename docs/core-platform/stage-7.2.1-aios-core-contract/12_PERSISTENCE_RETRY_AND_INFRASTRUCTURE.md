# Persistence, Retry, and Infrastructure

- `AIOS CORE PERSISTENCE = NONE`.
- `AIOS CORE RETRY = NONE`.
- `NEW INFRASTRUCTURE = NONE`.

No PostgreSQL/schema/table/ORM, state store, event log, cache, vector DB,
automatic retry/reroute/backoff, broker, queue, worker, network client, Redis,
Kafka, RabbitMQ, NATS, Celery, LLM/Ollama, external service, or Docker service
is permitted.
