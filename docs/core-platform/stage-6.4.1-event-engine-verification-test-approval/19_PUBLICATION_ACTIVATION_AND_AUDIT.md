# Publication, Activation, and Audit

After this governance-only package is reviewed and normally merged:

- the test approval is **PUBLISHED**;
- Project Owner approval is **ACTIVE**; and
- Stage 6.4.1 is **VERIFICATION/TEST APPROVED — READY TO VERIFY**.

The merge audit must prove that only this governance directory entered `main`,
`HEAD == main == origin/main`, and the worktree is clean. Activation authorizes
only the later one-file test implementation and required verification. It does
not begin that implementation in this task.
