# Resolved and Deferred Decisions

## Defined now

Purpose; Route meaning; upstream/downstream position; stateless v1; async
direction; determinism; Brain-boundary-only positive target;
invalid/unsupported stop; no persistence/retry/intelligence; and
Brain/Memory/Specialist/business separation.

## Deferred to Stage 7.2.1

Exact input type; EventEnvelope/EventDeliveryResult usage; output DTO; API name
and signature; validation rules; failure codes; route-target representation;
runtime dependency list; implementation paths; and tests.
