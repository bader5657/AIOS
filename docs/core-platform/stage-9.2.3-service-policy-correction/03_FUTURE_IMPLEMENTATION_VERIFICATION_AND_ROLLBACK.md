# Future Implementation, Verification, and Rollback

## Repository implementation eligibility

After this correction is active, a separate Stage 9.2.3 implementation
approval may authorize changes only to:

- `deploy/systemd/aios.service`; and
- `tests/unit/core_platform/test_aios_systemd_service.py`.

The future artifact should add only:

```ini
Environment=PYTHONPYCACHEPREFIX=/opt/aios/runtime/cache/pycache
ReadOnlyPaths=/opt/aios-src
```

The focused test should verify those exact directives, preservation of every
existing policy value, absence of additional source write authority, absence
of `PYTHONPYCACHEPREFIX` from `runtime.env`, exactly one `ExecStart`, and no
containerization, business retry, migration, logging, or monitoring change.

This package does not authorize those repository edits.

## Future controlled VPS scope

A still-separate production approval may later authorize only:

1. create `/opt/aios/runtime/cache/pycache`;
2. set `aiosadmin:aiosadmin` ownership and mode `0750` on that exact path;
3. preserve or disposition existing generated source bytecode using an exact,
   reviewed path procedure without wildcard deletion;
4. install the reviewed corrected unit;
5. run `daemon-reload`;
6. perform one controlled stop/start;
7. prove exactly one Telegram polling process;
8. exercise normal imports/runtime operation;
9. prove the source checkout remains clean; and
10. prove generated bytecode appears only under the approved runtime cache.

No reboot is required because Stage 9.2.2 already proved reboot activation.
No source, application semantics, runtime environment file, venv, PostgreSQL,
Storage, Docker, migration, or polling behavior may change.

## Separation verification criteria

Acceptance requires:

- exact approved source HEAD;
- no tracked, staged, untracked, or ignored runtime residue in source;
- normal service execution does not make source dirty;
- source is read/execute-only from the service perspective;
- runtime bytecode is confined to the approved cache hierarchy;
- the service remains active with exactly one polling process;
- PostgreSQL and Storage state remain unchanged and operational; and
- systemctl/journalctl observability remains intact.

## Existing bytecode disposition

Existing generated paths retain this exact classification:

`KNOWN GENERATED SOURCE-TREE RUNTIME RESIDUE — NON-BLOCKING FOR STAGE 9.2.2`

A later production approval may enumerate exact paths for quarantine or
removal after the service is stopped and before source-clean verification.
Broad or wildcard deletion is prohibited.

## Rollback

If the corrected service fails:

1. stop it under the same controlled one-poller procedure;
2. restore the prior Stage 9.2.2 approved unit;
3. run `daemon-reload`;
4. restart once and prove the one-poller topology;
5. leave the disposable runtime cache in place unless separately authorized;
6. do not roll back source, `runtime.env`, venv, PostgreSQL, database/schema,
   Storage, or business data.
