# Retry and Persistence Exclusion Audit

There is no retry loop, backoff, reroute, retry counter, route log, database
storage, filesystem storage, route cache, route history, event store, vector
store, or persistent routing record.

`AIOS CORE RETRY = NONE`

`AIOS CORE PERSISTENCE = NONE`
