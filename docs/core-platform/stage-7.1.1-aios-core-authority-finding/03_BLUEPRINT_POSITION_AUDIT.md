# Blueprint Position Audit

The official pipeline places `AIOS Event Engine → AIOS Core → AIOS Brain →
Specialist Router`. The lifecycle names `Process → Route → Respond`.

This is position and lifecycle vocabulary authority. The Blueprint does not
define Route inputs, outputs, decisions, algorithm, API, validation, failures,
state, concurrency, or invocation semantics. Position is not executable
behavioral authority.
