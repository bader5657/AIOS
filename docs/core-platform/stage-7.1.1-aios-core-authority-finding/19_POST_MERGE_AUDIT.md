# Post-Merge Audit

After normal merge, verify the package exists on `main`, `HEAD == main ==
origin/main`, and the worktree is clean. The merge must contain only this
governance directory and no runtime, test, configuration, Stage 5, Stage 6,
Blueprint, Roadmap, or architecture change.

The only active outcome is the Stage 7.1.1 `UNDEFINED` authority finding and
eligibility to conduct, but not begin here, Stage 7.1.2.
