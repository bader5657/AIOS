# Historical REPLACE Evidence

The Stage 6.1.2 REPLACE disposition remains active. The implementation restores
none of the historical generic Event, dispatcher, registry API, mutable payload,
naive timestamp generation, synchronous-only processing, silent unknown-event
success, or old handler API.

Snapshot and registration-order behavior was implemented freshly under current
authority.
