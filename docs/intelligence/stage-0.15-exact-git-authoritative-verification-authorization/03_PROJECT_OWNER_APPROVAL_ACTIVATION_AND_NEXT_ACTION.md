# Project Owner Approval, Activation, and Next Action

I, as Project Owner, authorize the Stage 0.15 isolated verification environment
against the exact Git-authoritative implementation SHA:

`21aeed1ad0f87a3a28835a9aaf4b67a0f8fab44f`

I authorize `/usr/bin/python3` version 3.12.3, an isolated disposable venv,
repository-declared requirements, and exactly `pytest==8.4.2` as a
`TEST_ONLY_VERIFICATION_TOOL`.

No source modification, production-environment mutation, live inference, Core
wiring, Stage 0.16 work, or additional test packages are authorized.

After publication and clean synchronized-main verification, this authority
activates as:

`STAGE 0.15 EXACT GIT-AUTHORITATIVE VERIFICATION AUTHORITY APPROVED — READY FOR CONTROLLED ENVIRONMENT PREPARATION`

The exact next operator action is to prepare the authorized checkout, venv, and
evidence directory under the approved boundaries. Test execution follows only
as the separately controlled verification operation. Successful verification
enables Stage 0.15 evidence reconciliation and final closure only; Stage 0.16
remains unauthorized.
