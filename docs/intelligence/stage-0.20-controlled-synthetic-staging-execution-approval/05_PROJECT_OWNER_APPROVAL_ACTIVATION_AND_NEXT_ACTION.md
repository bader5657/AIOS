# Project Owner Approval, Activation, and Next Action

I, as Project Owner, authorize exactly one controlled Stage 0.20 synthetic
staging inference after an immediately passing operational safety preflight.

The execution must use the exact repository-owned semantic projector,
CoreToBrainMapper, Stage 0.19 staging composition, Stage 0.18 schema binding,
Brain receiver/invoker/provider stack, and the already-isolated Ollama/Qwen
runtime.

The exact synthetic text, deterministic correlation/request UUIDs,
CoreRouteResult, provenance, request count, no-retry/no-fallback behavior, cold
unloaded start, resource gates, source identity, and evidence requirements are
frozen by this package.

No runtime mutation, second request, benchmark, real user data, business
action, persistent Level B activation, production activation, Memory,
Specialist routing, or retry/fallback is authorized.

Publication requires a normal clean, mergeable governance-only PR into main,
without force or history rewrite. No inference occurs during publication.
After synchronized-main audit, authority activates as:

`INTELLIGENCE STAGE 0.20 CONTROLLED SYNTHETIC STAGING EXECUTION APPROVED — READY FOR IMMEDIATE PREFLIGHT AND ONE REQUEST`

The next operator task may perform only: immediate full preflight, exactly one
request, immediate postflight, one retained evidence record, and stop. Success
establishes controlled synthetic staging execution verification only; explicit
Level B activation remains separately governed.
