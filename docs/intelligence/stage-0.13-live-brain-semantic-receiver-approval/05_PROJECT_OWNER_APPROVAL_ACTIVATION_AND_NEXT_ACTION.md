# Project Owner Approval, Activation, and Next Action

## Project Owner approval

I, as Project Owner, authorize exactly one synthetic live staging invocation
beginning from one valid BrainInput and passing through the accepted
BrainSemanticReceiver, BrainInferenceInvoker, OllamaInferenceProvider, and
isolated Ollama/Qwen runtime.

The test may use a temporary bounded schema resolver/validator and direct
operator-side composition solely to prove receiver-to-runtime interoperability.

The exact approved correlation/request IDs must be matched by the execution
harness before inference.

No Core mapping/wiring, production composition, production inference, Memory,
Specialist routing, business action, retry, fallback, or persistent state is
authorized.

## Publication and activation

The allowed diff is this governance package only. Publication requires a
normal clean, mergeable PR into `main`, without force or history rewrite.
After merge and a synchronized clean governance-only audit, authority activates
as:

`INTELLIGENCE STAGE 0.13 LIVE BRAIN SEMANTIC RECEIVER APPROVED — READY FOR CONTROLLED EXECUTION`

Activation itself creates no checkout or harness and executes no inference.

## Stop conditions and next operator action

Stop and return to governance if exact source/import/ID authority cannot be
established, preflight is failed or indeterminate, a package/runtime/source/
limit/configuration change is needed, the single invocation fails or raises,
a second call/retry/fallback would be required, content would persist, or Core,
mapper, production, Memory, Specialist, business, composition-root, dependency,
or implementation changes become necessary.

The exact next operator action is:

`Intelligence Stage 0.13 — create and verify the exact-SHA temporary checkout, pass mandatory preflight and ID equality gate, execute exactly one approved BrainSemanticReceiver staging invocation, perform mandatory postflight, and return bounded evidence for separate acceptance/closure.`

No later stage is authorized here. After successful Stage 0.13 execution and
closure, controlling authority may evaluate the Core-to-Brain boundary mapper.
