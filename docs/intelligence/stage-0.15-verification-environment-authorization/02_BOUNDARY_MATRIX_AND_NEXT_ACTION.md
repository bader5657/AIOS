# Boundary, Intended Matrix, and Next Action

If the missing authorities are granted, the intended environment remains one
disposable isolated virtual environment and detached clean source checkout,
separate from `/opt/aios-src`, production Python, service import paths,
production secrets, business data, PostgreSQL, Telegram polling, and Ollama.

The future controlled run must cover the approved 17-item non-live matrix:
Stage 0.15 integration; CoreToBrainMapper; BrainInput; BrainSemanticReceiver;
BrainInferenceInvoker; Ollama adapter mock tests; inference contracts; Core and
Domain regressions; Stage 8 and Stage 9 gates; the full suite; compile/static;
dependency/import and prohibited-source audits; `git diff --check`; and the
exact one-path implementation audit.

Expected Stage 8 environment skips must be recorded by exact reason. Any test
failure must be retained without source repair. Evidence must record source and
blob identity, environment and Python identity, dependency versions, commands,
timestamps, counts, skips, failures, audits, and final classification without
secrets.

The environment must remain available until evidence is accepted; cleanup is
separately governed. Pre/post production safety checks remain mandatory. No
Stage 0.16 work is authorized.

The next action is a corrected, bounded authorization covering the exact
Git-authoritative source and the missing test-only tooling acquisition.

`STAGE 0.15 VERIFICATION ENVIRONMENT AUTHORIZATION BLOCKED`
