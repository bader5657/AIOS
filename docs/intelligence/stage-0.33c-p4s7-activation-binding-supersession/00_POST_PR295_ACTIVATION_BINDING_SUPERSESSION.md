# AIOS P4S7-R24 Activation Binding Supersession

Classification: `P4S7_POST_PR295_ACTIVATION_BINDING_SUPERSESSION_READY_FOR_REVIEW`

## Purpose and narrow supersession

This governance-only addendum supersedes **only** the stale activation
`expected_runtime_head` and `executor_sha256` bindings frozen before PR #295.
[Merged PR #292](https://github.com/bader5657/AIOS/pull/292) and its
[activation governance](../stage-0.33c-p4s7-post-merge-activation/00_POST_MERGE_ACTIVATION_CREATION.md)
remain immutable merged history. No historical document or binding is rewritten.
For future activation creation, the active values below take precedence over
those two historical values. All other activation bindings, safety controls,
publication requirements, and authority boundaries remain unchanged.

This addendum requires independent review and human merge. Review readiness is
not activation-creation authorization. No single document alone authorizes
activation creation, and this task stops at a new governance PR without merging.

## Historical and active bindings

| Binding | PR #292 historical value, preserved | Active value for future creation |
|---|---|---|
| `expected_runtime_head` | `abb11a25dd7810c4742ae2a724f0c1bd89de3efd` | `3c7625123289bc9cc349b4e1b8198b88660f8401` |
| `executor_sha256` | `f649b19c09c9d719b11577b14bfad0ce458f5c4dd6ccfd88c6727594787c81aa` | `3eec6a3b0cf0e1d9a768c15bf445876d3a463f38528a8fb4046ea27ba750ed97` |

Future activation creation must use these exact active values. The old values
are retained solely as history and must not be used for a new activation.
Require:

`actual executor SHA == policy-bound SHA == activation executor_sha256`

All three must equal
`3eec6a3b0cf0e1d9a768c15bf445876d3a463f38528a8fb4046ea27ba750ed97`.
The executor is
`docs/intelligence/stage-0.33c-step4-one-shot-runtime-install-authority/one_shot_install.py`
under `/opt/aios-src`. Runtime executor bytes must also equal the frozen HEAD
blob. Any mismatch is STOP; no executor or policy repair is authorized here.

Merging this governance addendum must not advance the runtime checkout. The
active expected runtime HEAD stays frozen at PR #295's merge commit; the later
governance merge is not a replacement runtime HEAD. Any runtime advance requires
separate governance, not an opportunistic change to this binding.

## Unchanged closed activation schema

Exactly nine keys remain required, with no missing, extra, or duplicate keys.
All values are JSON strings except the exact JSON integer `pr_number`.

| Key | Active frozen value or unchanged creation rule |
|---|---|
| `schema_version` | `aios-stage-0.33c-p4s7-r13-post-merge-activation-v1` |
| `authority_id` | `9d29c855-0f23-4539-a9b9-2e17dc89c49d` |
| `pr_number` | JSON integer `289` |
| `reviewed_head_sha` | `209618b845c844ad08915853fa1dfa07c1c9897a` |
| `authority_merge_sha` | `a22ac1f13954040d6182ab2fa0614952f3e4f2b6` |
| `expected_runtime_head` | `3c7625123289bc9cc349b4e1b8198b88660f8401` |
| `executor_sha256` | `3eec6a3b0cf0e1d9a768c15bf445876d3a463f38528a8fb4046ea27ba750ed97` |
| `policy_reference` | `docs/intelligence/stage-0.33c-step4-one-shot-runtime-install-authority/00_ONE_SHOT_RUNTIME_INSTALLATION_AUTHORITY.md` |
| `activated_at_utc` | Actual creation-time UTC only, `YYYY-MM-DDTHH:MM:SS.ffffffZ`; never prefill, backdate, or reuse. |

The timestamp must be a valid UTC calendar time with exactly six fractional
ASCII digits and uppercase `Z`. Booleans, floats, exponent notation, or strings
are invalid substitutes for integer `289`. No activation JSON instance or
creation timestamp is generated by this addendum.

## Canonical serialization preserved

The [merged PR #293 contract](../stage-0.33c-p4s7-activation-serialization/00_ACTIVATION_CANONICAL_SERIALIZATION_CONTRACT.md)
and [merged PR #294 enforcement](https://github.com/bader5657/AIOS/pull/294)
remain binding. The sole serializer is UTF-8 with `ensure_ascii=False`,
`sort_keys=True`, `separators=(",", ":")`, and `allow_nan=False`:

```python
semantic_bytes = json.dumps(
    activation, ensure_ascii=False, sort_keys=True,
    separators=(",", ":"), allow_nan=False,
).encode("utf-8")
transport_bytes = semantic_bytes + b"\n"
```

This is a specification, not a creation command. Semantic bytes contain
canonical JSON without LF, BOM, or surrounding whitespace. Transport bytes add
exactly one ASCII LF (`0x0A`), with no CRLF, second LF, or trailing spaces. Do not
strip, normalize, or repair invalid bytes. Exact semantic-byte comparison,
duplicate rejection, closed-schema and value/type checks remain enforced before
claim. No activation hash field or other schema extension is introduced.

## PR #295 Git enforcement and R23 evidence

The [merged PR #295 amendment](../stage-0.33c-p4s7-r22a-git-safe-directory/00_GIT_SAFE_DIRECTORY_AMENDMENT.md)
is part of the active execution contract. Both executor Git helpers use exactly:

`/usr/bin/git -c safe.directory=/opt/aios-src -C /opt/aios-src ...`

The trust setting is process-local and literal. No wildcard, global/system
configuration mutation, alternate trusted repository, or ownership-test flag is
permitted. The executor subprocess environment remains exactly
`{"PATH": "/usr/bin:/bin"}`.

The supplied R23 live-root minimal-environment verification records:

| R23 check | Result |
|---|---|
| `rev-parse` | PASS |
| Clean-tree verification | PASS |
| Merge ancestry | PASS |

These are incorporated R23 evidence, not root checks newly performed by R24.
They do not replace fresh live-root verification immediately before future
activation creation.

R24 read-only inspection on 2026-09-26 (Asia/Jakarta) independently confirmed
the `/opt/aios-src` HEAD equals the active frozen HEAD, the runtime executor
SHA-256 equals both the current policy binding and the active frozen digest,
and the activation path is absent by `lstat`. The local checkout at the frozen
HEAD also has that executor digest. No private-source bytes were read and no
fresh privileged private-source or approval-freshness verification is claimed.

## Required merged lineage

All of these merge commits must be contained in the active expected runtime
HEAD. R24 verified each through local Git
`merge-base --is-ancestor <merge-sha> 3c7625123289bc9cc349b4e1b8198b88660f8401`
in the fetched development repository; every command returned exit status 0.
GitHub merge metadata identifies the PRs; local Git ancestry supplies the proof.

| Required component | Merge SHA | R24 local ancestry |
|---|---|---|
| PR #289 | `a22ac1f13954040d6182ab2fa0614952f3e4f2b6` | PASS |
| PR #290 | `327732e611b6d6bb3b88c33775e320d2d5173862` | PASS |
| PR #291 | `c58dcb92f49c529c00d6aeb09efe7fab7d00a47e` | PASS |
| PR #292 | `d271fd4251d6f4c9eec091080a78948771d2cc95` | PASS |
| PR #293 | `c8e2bdf9180ce71cc1dda5139ae786756e2dd22a` | PASS |
| PR #294 | `abb11a25dd7810c4742ae2a724f0c1bd89de3efd` | PASS |
| PR #295 | `3c7625123289bc9cc349b4e1b8198b88660f8401` | PASS |

Future pre-creation verification must repeat every ancestry check locally in
`/opt/aios-src` using the exact process-local Git contract above. No component
may be omitted. Preserve the reviewed-authority document byte-equality check
between the frozen PR #289 reviewed head and its authority merge.

## Fixed path, fresh gates, and unchanged expiry

The only activation path remains:

`/opt/aios/runtime/intelligence/production-candidate-create/stage-0.33c/p4s7-r13-post-merge-activation.json`

It must remain absent throughout R24. No alternate path or override is allowed.
Future creation must freshly revalidate every PR #292 pre-creation gate with
this addendum's active bindings and expanded lineage, immediately before creation:

1. Independent review and human merge of this addendum, the complete merged
   authority composition, and the preserved PR #289 review/merge evidence.
2. Actual runtime HEAD exactly equal to the active frozen value, with a clean
   tracked, staged, and untracked tree; no implicit runtime synchronization.
3. Actual runtime executor SHA, policy SHA, and proposed activation SHA exactly
   equal to the active digest, with executor bytes equal to the HEAD blob and
   the existing policy authority-ID/classification checks preserved.
4. Local ancestry for every PR #289 through #295 merge listed above and the
   unchanged reviewed-authority byte binding.
5. Live root Git checks for HEAD, clean tree, and ancestry in the exact PATH-only
   environment with `-c safe.directory=/opt/aios-src`; no persistent trust changes.
6. Fresh privileged private-source path, metadata, semantic/transport size and
   hash, package/approval identity, TF-A, and Model B verification under the
   unchanged merged authority and policy. The source parent remains a real
   non-symlink `root:root` directory mode `0700`; both source files remain
   regular, non-symlink, single-link `root:root` mode `0400`. Never expose private
   content, weaken permissions, regenerate, or modify sources.
7. Fresh approval-freshness verification from the exact unchanged private
   approval. Preserve expiry **`2026-09-26T21:24:52.273127Z`**, without renewal,
   replacement, or extension. Require actual UTC `now` strictly earlier than
   both that expiry and the validated private approval `not_after_utc`.
   Equality, expiry, mismatch, invalid time, or inaccessible evidence is STOP.
8. Activation absent; final `approved-input.json` and
   `approved-input-approval.json` absent under the fixed activation parent.
9. Claim and result absent in that parent's `runtime-sync-evidence` directory:
   `step4-install-authority-9d29c855-0f23-4539-a9b9-2e17dc89c49d.json` and
   `step4-install-authority-9d29c855-0f23-4539-a9b9-2e17dc89c49d.json.result.json`.
   Require UNUSED state with no prior ambiguous or durable consumption history.
10. No staging entries beginning `.approved-input.json.stage-` or
    `.approved-input-approval.json.stage-` in the runtime parent.
11. Preserve no-follow path-component safety and directory contracts: runtime
    parent `root:aiosadmin` mode `0750`, evidence directory
    `aiosadmin:aiosadmin` mode `0700`, both real directories without symlink traversal.

Absence checks must use no-follow directory-relative lookup; dangling symlinks
and every other existing entry type count as present. Any failed, inaccessible,
uncertain, or stale gate means **STOP BEFORE CREATION**. No cleanup, adoption,
reset, deletion, repair, replacement, or automatic retry is granted. Historical
R23 PASS cannot satisfy fresh gates. Creation does not reserve approval freshness;
final pre-attempt verification must check freshness again.

Future exclusive publication and independent verification retain all PR #292
controls: no overwrite, root-owned regular single-link file mode `0400`, complete
write, file `fsync`, close all writable descriptors, parent directory `fsync`,
then read-only/no-follow reopen and exact identity, metadata, bytes, length, hash,
schema, binding, and timestamp verification. No publication occurs in R24.

## Composite authority, boundaries, and next action

Final activation authority is the composition of:

- Merged PR #292 activation governance, preserving its history and controls.
- Merged PR #293 canonical serialization governance.
- Merged PR #294 serialization enforcement.
- Merged PR #295 Git safe-directory enforcement.
- This separately reviewed and human-merged supersession/addendum.
- Fresh pre-creation verification evidence satisfying every gate above.

This composition retains the underlying PR #289, #290, and #291 authority and
lineage requirements. No single document alone authorizes activation creation.
Historical statements about pending reviews, merges, or enforcement remain
historical; the merged lineage and supplied R23 evidence above establish the
post-PR #295 baseline without editing that history.

| R24 boundary | Result |
|---|---|
| Activation created | NO |
| Runtime modified | NO |
| Installer executed | NO |
| Installation claim/result created | NO |
| Runtime targets created | NO |
| Private sources modified | NO |
| Services restarted | NO |
| PostgreSQL contacted | NO |
| Harness invoked | NO |
| Candidate created | NO |
| `authorization.json` created | NO |
| Step 5 authorized | NO |

Next official action: independent review of this narrow governance-only PR,
followed by human merge if accepted. No automatic merge. Future activation
creation requires the full composite authority and fresh verification; it is a
separate stage. Independent activation verification, final pre-attempt checks,
and any later one-shot installation remain separate governed actions. STOP.
